using System;

namespace Functions.StravaDto
{
    public record Activity(
        ulong id,
        int resource_state,
        AthleteInfo athlete,
        string name,
        double distance,
        int moving_time,
        int elapsed_time,
        double total_elevation_gain,
        string type,
        DateTime start_date,
        DateTime start_date_local,
        string timezone,
        double utc_offset,
        string location_city,
        string location_state,
        string location_country,
        int achievement_count,
        int kudos_count,
        int comment_count,
        int athlete_count,
        int photo_count,
        Map map,
        bool trainer,
        bool commute,
        bool manual,
        bool _private,
        string visibility,
        bool flagged,
        string gear_id,
        double?[] start_latlng,
        double?[] end_latlng,
        double? start_latitude,
        double? start_longitude,
        double average_speed,
        double max_speed,
        bool has_heartrate,
        bool heartrate_opt_out,
        bool display_hide_heartrate_option,
        double elev_high,
        double elev_low,
        long? upload_id,
        string upload_id_str,
        string external_id,
        bool from_accepted_tag,
        int pr_count,
        int total_photo_count,
        bool has_kudoed,
        int? workout_type,
        int average_temp,
        double average_watts,
        double kilojoules,
        bool device_watts,
        double average_heartrate,
        double max_heartrate,
        double average_cadence);

    public record AthleteInfo(
        ulong id,
        int resource_state);

    public record Map(
        string id,
        string summary_polyline,
        int resource_state);

    public record ActivityDetail(
        ulong id,
        AthleteInfo athlete,
        int resource_state,
        string external_id,
        long? upload_id,
        string name,
        double distance,
        int moving_time,
        int elapsed_time,
        double total_elevation_gain,
        string type,
        DateTime start_date,
        DateTime start_date_local,
        string timezone,
        double utc_offset,
        double?[] start_latlng,
        double?[] end_latlng,
        int achievement_count,
        int kudos_count,
        int comment_count,
        int athlete_count,
        int photo_count,
        bool trainer,
        bool commute,
        bool manual,
        bool _private,
        bool flagged,
        string gear_id,
        bool from_accepted_tag,
        double average_speed,
        double max_speed,
        double average_cadence,
        int average_temp,
        double average_watts,
        int weighted_average_watts,
        double kilojoules,
        bool device_watts,
        bool has_heartrate,
        int max_watts,
        double elev_high,
        double elev_low,
        int pr_count,
        int total_photo_count,
        bool has_kudoed,
        int? workout_type,
        string description,
        double calories,
        bool hide_from_home,
        string device_name,
        string embed_token,
        bool segment_leaderboard_opt_out,
        bool leaderboard_opt_out);
}
